# defmodule Day3_Bis do

#   def modify_map(map) do
#     length = Enum.map(map.numbers.numbers_list, &(String.split(&1, "", trim: true)))
#     |> Enum.map(&(length(&1)))


#   end

#   def group_index(map, length) do
#     Enum.map(map.)
#     Enum.take_every(map.numbers.numbers_list.index, )
#   end

# end
